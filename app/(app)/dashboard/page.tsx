import { DashboardView } from "@/components/dashboard/dashboard-view";

export const metadata = {
  title: "Dashboard · Nevados CRM",
};

export default function DashboardPage() {
  return <DashboardView />;
}
